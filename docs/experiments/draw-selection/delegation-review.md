# External agent work and coordinator review

Actual Claude Code and Cursor sessions were used, not substitutes described as
those products. Private execution receipts are retained outside the repository.

## Claude Code

Claude Opus Medium handled bounded Oracle parsing and public regression fixtures.
The completed public-input session records `claude-opus-5` with auxiliary Haiku;
receipt: `generator-draw-agents/claude-public-parser/agent-result.json`.

Automatic approval review rejected sending the initial repository-source payload.
The task was safely completed using public Oracle fixtures and a generic task in
an isolated directory, followed by local integration. No unresolved approval is
needed for this completed alternative.

Coordinator review corrected over-credit for hand replacement, temporary effects,
control transfer, unmodeled upkeep/activation costs, conditional tribal triggers,
finite counters and global restrictions. Public negative fixtures preserve those
failures. See `../selection-validation/draw-facts.md` for parser details. Agent
output was not accepted as evidence of gameplay correctness on its own.

## Cursor

A real Cursor Docker source-only session using Grok 4.6 High implemented pure
multiset selection receipts and tests. Receipt:
`generator-draw-agents/cursor-result.json`. The source snapshot was isolated from
private runtime data. The receipt module's 21 tests, isolated mypy and Ruff passed.

Independent follow-up review added adversarial selection tests and exposed hard
versus median type-floor confusion, unsafe cuts, repeated expensive audits,
self-enabling token credit and duplicate candidates. The coordinator corrected
these and ran the full suite. The remaining dependence-quota optimization limit
is reported as unresolved, not papered over.

## Integration ownership

The coordinator implemented contextual scoring, pool retention, early reservation,
repair integration, final audits and conservative scope guards, then reviewed real
build differences. Delegated experiment execution used immutable source snapshots
and preserved failed as well as successful results. All claims in results.md are
bounded by those receipts and explicit manual review limitations.
