# Cursor package: rule matrix acceleration
Own ONLY src/sabermetrics/analytics/synergy_matrix.py, tests/test_synergy_matrix_acceleration.py, scripts/benchmark_synergy_rules.py.

Replace nested calls to _match_rules for all pairs with precomputed per-card/per-rule trigger and payoff masks, combining masks with vectorized numpy operations. Keep _match_rules and _card_matches_clause as reference semantics; preserve exact float32 matrix, symmetry, zero diagonal, strength max behavior, arbitrary clause combinations. Avoid N*N*R allocation. O(N*R) semantic evaluations; dense output stays O(N*N). Expose build_rule_matrix(candidates,rules) so benchmark can compare to reference without loading embeddings. Handle empty cards/rules, invalid metadata exactly as reference does (do not silently invent metadata). Benchmark deterministic synthetic varied oracle facts, report reference/new seconds and equality, at 200+ candidates. Do not alter scoring weights or add dependencies. Tests should verify equality over diverse and randomized shapes, not mirror implementation. Keep APIs stable. Implement, run focused tests, report changed files and limitations.
