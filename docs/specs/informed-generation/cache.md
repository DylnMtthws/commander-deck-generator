# Cursor package: prepared embeddings and empty retrieval
Own ONLY src/sabermetrics/analytics/embeddings.py, src/sabermetrics/reference_layer/retriever.py, src/sabermetrics/runtime/prepare_embeddings.py, tests/test_prepared_embeddings.py.

Add optional persistent SQLite embedding cache to EmbeddingService, activated by SABER_EMBEDDING_CACHE path and SABER_EMBEDDING_REVISION (explicit model version key). Key includes model name, revision, exact text hash, dimensionality/format metadata. Pass revision to SentenceTransformer when set; do not claim model pinned when unset. Preserve existing in-memory API and callers. Deduplicate repeated texts in batch, reuse disk between fresh service instances, short-lived connections, concurrency safe, finite float32 vectors/dimension validation. Corrupt or incompatible entries recompute safely, not silently score zero. Cache absence/errors allow normal compute with bounded diagnostic; never pickle. Make no model network calls in tests.

New operator CLI python -m sabermetrics.runtime.prepare_embeddings --db PATH --cache PATH [--batch-size 64], opt-in explicit public-card preparation: canonical oracle IDs/text only, incrementally process batches using service, report counts; do not read user tables; never precompute in request path. No schema change to application database. Cache persists separately under configured path. ReferenceRetriever must return [] for an empty available embedding corpus BEFORE constructing an encoder/indexer. Preserve fallback text search behavior.

Tests: restart persistence, text/model/revision invalidation, duplicate text batching, corruption/dimension recovery, missing/unwritable cache graceful compute, no encoder for empty collection. Do not modify config.py or other runtime files. Implement fully and run focused tests.
