# Coordinator selection contract

Compile conservative card facts from exact Oracle/type text; annotate discovery tags separately. Do not let substring matches to graveyard recursion, transformed-face mana, draft choices, or same-name tutors satisfy unrelated requirements. Fail closed on confirmed nonfunctional prerequisites; report unknown coverage.

Landfall: reserve affordable enablers and payoffs before Pareto, preserve through every pass, report exact actual/required capability counts. Compare balanced/resources/payoffs recipes with shared protected infrastructure; require valid size/budget/singleton, preserve interaction minima, score verified plan coverage then the existing structural objective. No win claim. Fix stale swap baseline and unify displayed/validation functional counts.

Review: maximum 18 selected risk cards + 8 menu alternatives in one batched call. Alternatives need their own passing verdict, final legality/budget/role checks and unique identity. No recursive replacement chain. Missing or rejected verdicts with no eligible alternative leave a flagged slot. Persist actual review coverage; do not claim exhaustive model review.

Simulation: same-spell basic-land color variants only may use the new narrow C++ scenario for ranking. No spell or interaction removed for goldfish speed. Conservative fixed-budget paired differences, fresh confirmation seeds, 15-second total caller budget. Unsupported land mechanics or unavailable service are explicit; existing deck generation remains usable without a simulator.

The controlled C++ scenario uses exact mana payment but excludes all noncommander spells, opponents, combat and mulligans. This release does not claim arbitrary landfall goldfishing. Any broader engine comparison requires an expanded tested capability contract.
