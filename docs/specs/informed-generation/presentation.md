# Cursor package: render generation evidence
Own ONLY src/sabermetrics/ui/templates/_generation_evidence.html, minimal include insertion in deck_view.html, tests/test_generation_evidence_view.py.

Render an optional panel for deck.rationale.intelligence (dict). Legacy decks without the field must render unchanged. Contract: version; plan={archetype,status,requirements:{role:count},counts:{role:count},missing:{role:{actual,required}}, evidence:[{card,requirement,reason}], limitations:[string]}; findings:[{code,card,message,severity}]; decisions:[{card,requirement,reason}]; simulation={status,reason?,scope?,baseline?:{games,turns,commander_cast_count,lands_mean,commander_draws_mean,assumptions:[str]},comparisons:[{variant,delta,interval:[low,high],metric,improved}],selected?,confirmation?}.

Show compact Strategy plan and supporting selected cards; missing requirements visible. Simulation unavailable/unsupported is plain language, never a zero score. Measured output must display scope and assumptions alongside probabilities, label commander affordability in land-only scenario (not win rate). Display games and turn, but don't expose hashes, raw card IDs, JSON, internal schema names. Do not show an unsupported claim that overall deck strength improved. Keep long findings collapsed using details, max 20 display with remainder count. Escape all data, no |safe. Use existing styles. Tests render partial directly with Jinja/Flask fixtures for missing evidence, measured result, unsupported result and malicious card/reason text. No routes/auth/db changes. Do not deploy, commit, fetch dependencies, or send any data. Implement and test fully.
