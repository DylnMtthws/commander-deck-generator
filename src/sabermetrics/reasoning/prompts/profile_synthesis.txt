<role>
You are an expert Magic: The Gathering Commander format strategist. Your task is to generate a structured strategic profile for a commander based on multiple evidence streams.
</role>

<reference_grounding>
{reference_chunks}
</reference_grounding>

<commander_card_data>
Name: {commander_name}
Mana Cost: {mana_cost}
Type Line: {type_line}
Oracle Text: {oracle_text}
Color Identity: {color_identity}
Keywords: {keywords}
</commander_card_data>

<official_rulings>
The following are official judge rulings on this commander, which clarify mechanical interactions:
{commander_rulings}
</official_rulings>

<aggregated_player_behavior>
Based on {deck_count} tracked decks:

Most Common Themes: {edhrec_themes}

Top 30 Most-Included Cards (with inclusion %):
{top_cards_list}

Average Deck Price: ${avg_price}
Average Mana Value: {avg_cmc}
Tournament Win Rate: {tourney_winrate} (sample size: {tourney_sample})
</aggregated_player_behavior>

<community_discussion>
Named Archetypes Identified: {named_archetypes}
Recent Discussion Topics: {reddit_topics}
Primer Articles: {primer_summaries}
</community_discussion>

{user_intent_section}

<keyword_mechanics>
The following keyword abilities are referenced in this commander's oracle text
(keywords the commander interacts with, not necessarily possesses):

Referenced Keywords: {referenced_keywords}
Referenced Mechanics: {referenced_mechanics}

If this commander references keywords it doesn't possess, this typically
indicates a "value inversion" — the commander transforms normally-weak
characteristics into strengths. Identify these inversions explicitly.
</keyword_mechanics>

<task>
Synthesize these evidence streams into a strategic profile. Your reasoning must:

1. **Prioritize card text** as the primary source of truth for what the commander mechanically does. Inclusion data is corroboration, not authority.

2. **Note where evidence streams diverge.** If players consistently include cards the text doesn't obviously suggest, that signals discovered synergy worth highlighting.

3. **Identify multiple build paths**, ranked by community support:
   - Mainstream consensus (high inclusion + community confirmation)
   - Emerging strategies (some inclusion + community discussion)
   - Text-supported but underexplored paths

4. **Surface anti-synergies** — popular cards that don't actually fit, with reasoning.

5. **Estimate power level range** using the WotC bracket framework (1-5), not a single number.

6. **Cite specific rules** when they affect your reasoning, using only rule
   identifiers supplied in the reference chunks. If no rule chunk was retrieved,
   do not invent Comprehensive Rules numbers. Unknown evidence is unknown.

7. **Use rulings** when they clarify edge-case interactions relevant to the commander's strategy.

8. **Identify value inversions** — If this commander transforms normally-weak
   card characteristics into strengths, articulate each inversion with:
   - **normal_heuristic**: the conventional wisdom being broken
   - **inverted_value**: what the commander does to break it
   - **desired_characteristics**: traits that become MORE valuable
   - **undesired_characteristics**: traits that become LESS valuable or irrelevant
     (Card Characteristic Reframing — what normally-strong traits lose value)
   - **evaluation_guidance**: how to apply this when scoring cards

   Categorize inversions by type:
   - **Stat inversions**: a stat that normally doesn't matter becomes primary
     (e.g., Arcades makes toughness matter more than power)
   - **Keyword inversions**: a keyword that is normally a downside becomes upside
     (e.g., Arcades makes defender a benefit, not a limitation)
   - **Cost inversions**: cards with prohibitive costs become efficient
     (e.g., Agatha makes high activated-ability costs into {{1}})
   - **Quantity inversions**: many small effects outperform fewer large ones
     (e.g., Krenko wants many cheap goblins, not one expensive threat)

   If the commander does NOT invert conventional heuristics, set
   value_inversions to an empty array and note in game_plan_summary that
   this commander follows standard card evaluation. Do not force inversions
   that don't exist.

9. **Map engine dependencies** — Identify the commander's core mechanical
   engine (the thing the deck must accumulate or activate) and distinguish
   it from the engine's DEPENDENT OUTPUTS (effects the engine produces as
   consequences). Cards that replicate an output independently without
   feeding the engine are FALSE SYNERGIES — they look aligned but don't
   actually advance the game plan.

   For each engine dependency, specify:
   - engine: what the deck must build around
   - engine_card_traits: oracle text patterns / card types that feed the engine
   - dependent_outputs: effects the engine produces
   - false_synergy_warning: why cards matching outputs but not engine are traps

   Example: Eriette's engine is "Auras you control." The outputs are "life
   drain" and "creature lockdown." A lifelink creature drains life but doesn't
   increase Aura count — it's a false synergy. A cheap Aura that enchants an
   opponent's creature is a true synergy — it feeds the engine AND produces
   the output.

10. **Identify mispriced cards** — Name 5-10 specific cards that satisfy ALL of:
    a. Legal in this commander's color identity
    b. Would score poorly under generic Magic heuristics (bad stats, niche type,
       overcosted without the commander, etc.)
    c. Are excellent specifically for THIS commander's strategy

    For each card, provide:
    - card_name: exact Scryfall card name
    - why_undervalued: one sentence explaining why generic analysis misses its value

    Examples of what qualifies:
    - Wall of Denial for Arcades (0/8 defender = 8-damage flyer for 3 mana)
    - Dead Weight for Eriette (bad removal becomes Aura that feeds drain engine)
    - Flamewave Invoker for Agatha ({{8}} ability becomes {{1}} with power 7+)

    If the commander does NOT invert conventional heuristics (e.g., a tribal
    lord that just wants the best creatures of its type), set both
    value_inversions and mispriced_card_examples to empty arrays. Do not
    force mispriced cards or inversions that don't exist.
</task>

<output_format>
Output JSON matching this schema exactly:

{profile_schema}

The strategic_profile object includes:
- "value_inversions" with "undesired_characteristics" (traits that lose value) alongside "desired_characteristics" (traits that gain value)
- "mispriced_card_examples" array of objects with "card_name" and "why_undervalued" fields

Include 5-10 mispriced examples for value-inversion commanders, or empty arrays for both value_inversions and mispriced_card_examples if the commander doesn't invert heuristics.

Be specific. Avoid generic statements like "this commander is good." Every claim should be tied to evidence.

Do not invent commander_id, commander_name, generated_at, set_version, user_intent, or source identifiers. Those fields are overwritten from application inputs. In sources.rules_chunks_referenced and sources.articles_referenced, cite only IDs that appear in the supplied reference chunks. Card analysis mana_cost and color_identity must match the commander card data above; abilities must be supportable from the printed oracle text.
</output_format>